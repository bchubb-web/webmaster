# 00 - Setup

## publish dockerfile
webmaster comes with a docker compose setup out of the box, to publish these files to your project, run the following command:

```bash
bash ./vendor/bchubb-web/webmaster/bin/publish.sh
```

this will copy the Dockerfile and docker-compose.yml files to the root of your project, allowing you to get a development environment up and running quickly.
